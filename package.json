{
  "homepage": "https://gaunaL.github.io/Helloword",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
